{
  "name": "WhisperLiveKit Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11"
    }
  },

  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y ffmpeg",

  "postCreateCommand": "pip install -e . && pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install websockets faster-whisper",

  "forwardPorts": [8000],

  "portsAttributes": {
    "8000": {
      "label": "WhisperLiveKit Server",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylance"
      ]
    }
  }
}
